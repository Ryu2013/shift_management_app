<p style="color: green"><%= notice %></p>

<% content_for :title, "user clients" %>

<h1>user clients</h1>

<div id="user_clients">
  <% @user_clients.each do |user_client| %>
    <%= render user_client %>
    <p>
      <%= link_to "Show this user client", user_client %>
    </p>
  <% end %>
</div>

<%= link_to "New user client", new_user_client_path %>
