<p style="color: green"><%= notice %></p>

<% content_for :title, "User needs" %>

<h1>User needs</h1>

<div id="user_needs">
  <% @user_needs.each do |user_need| %>
    <%= render user_need %>
    <p>
      <%= link_to "Show this user need", user_need %>
    </p>
  <% end %>
</div>

<%= link_to "New user need", new_user_need_path %>
