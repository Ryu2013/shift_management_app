<h1>お客様一覧</h1>

<div>
  <label for="resource_selector">表示を切り替える:</label>
  <%= select_tag :resource_selector,
      options_for_select([
        ['従業員', office_employees_path(office_slug: @office.slug)],
        ['お客様', office_clients_path(office_slug: @office.slug)]
      ], office_clients_path(office_slug: @office.slug)),
      onchange: 'Turbo.visit(this.value)' %>
</div>

<% if @clients.any? %>
  <table>
    <thead>
      <tr>
        <th>氏名</th>
        <th>住所</th>
      </tr>
    </thead>
    <tbody>
      <% @clients.each do |client| %>
        <tr>
          <td><%= client.name.presence || '-' %></td>
          <td><%= client.address.presence || '-' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>お客様の登録がまだありません。</p>
<% end %>
