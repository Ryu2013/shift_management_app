<%= turbo_frame_tag dom_id(@client_need) do %>

<h1><%= t(".heading") %></h1>

  <%= render "form", client_need: @client_need %>
  <%= link_to "キャンセル", root_path, data: { turbo_frame: "_top" } %>
<% end %>